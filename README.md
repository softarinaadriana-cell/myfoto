# Focus — To-Do List

A clean, responsive to-do list with local browser persistence.

## Features
- Add, complete, and delete tasks
- All / Active / Completed filters
- Clear completed tasks
- Progress indicator
- Responsive dark UI
- Tasks persist with `localStorage`

Open `index.html` directly or deploy the repository as a static site.